<div style="{{ styles.container }}">
  <div style="{{ styles.base }}">
    <span style="{{styles.headerSuccess}}">{{"Building" if isBuilding else "Uploading"}} package</span> <span style="{{ styles.code }}">{{pkgInfo.name}}=={{pkgInfo.version}}</span> from <span style="{{ styles.code }}">{{ path }}</span>
  </div>
</div>
