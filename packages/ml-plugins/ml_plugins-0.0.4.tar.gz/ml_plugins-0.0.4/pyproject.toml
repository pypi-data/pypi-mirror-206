[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ml_plugins"
version = "0.0.4"
dependencies = [
    "metaflow"
]

[tool.setuptools.packages.find]
where = ["plugins"]

