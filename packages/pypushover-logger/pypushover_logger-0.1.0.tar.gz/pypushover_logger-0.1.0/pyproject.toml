[tool.poetry]
name = "pypushover-logger"
version = "0.1.0"
description = "A Pushover logging handler"
authors = ["Jeffrey Muller <jeffrey.muller92@gmail.com>"]
readme = "README.md"
packages = [{ include = "pypushover_logger" }]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
