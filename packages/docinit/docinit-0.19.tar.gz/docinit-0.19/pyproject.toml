# Enable version inference
[tool.setuptools_scm]
local_scheme = "no-local-version"

# https://setuptools.readthedocs.io/en/latest/build_meta.html
[build-system]
requires = ["setuptools>=62", "wheel", "setuptools_scm[toml]>=3.4"]
build-backend = "setuptools.build_meta"