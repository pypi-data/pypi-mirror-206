[tool.poetry]
name = "interactive_cli"
version = "0.1.0"
description = ""
authors = ["miile7 <miile7@gmx.de>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
tabulate = "^0.9.0"

[tool.poetry.group.dev.dependencies]
black = "*"
mypy = "*"
flake8 = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
