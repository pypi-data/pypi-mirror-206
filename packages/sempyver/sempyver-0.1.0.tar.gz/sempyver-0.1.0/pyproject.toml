[tool.poetry]
name = "sempyver"
version = "0.1.0"
description = "A Python translation of the 🦋 Changesets project."
authors = ["ricardoleal20 <rick.leal420@gmail.com>"]
license = "MIT License"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
click = "~=8.1"
inquirer = "~=3.1"
colorama = "==0.4.6"
mypy = "~=1.0"
pylint = "~=2.15"
bandit = "~=1.7"
autopep8 = "~=2.0"
pytest = "~=7.2"
pytest-cov = "~=2.10"
faker = "~=17.6"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.sempyver]
version_file_path = "sempyver/_version.py"
